# 前端环境变量配置

# 后端 API 地址（根据部署环境修改）
# 开发环境
VITE_API_BASE_URL=http://localhost:8080

# 生产环境（部署后更新为实际后端 URL）
# VITE_API_BASE_URL=https://your-backend.vercel.app

# Firebase 配置（已在 src/config/firebase.ts 中硬编码，无需在此配置）
