# ====================================
# 本地开发配置（可选）
# ====================================
# 
# 用途：覆盖 .env.development 中的部分配置
# 优先级：.env.local > .env.development
#
# 使用场景：
# - 团队成员使用不同的本地后端端口
# - 临时切换到不同的 Firebase 项目测试
# - 个人调试配置（不影响团队其他成员）
#
# 使用方式：
# 1. 复制此文件为 .env.local
# 2. 根据个人需求修改配置
# 3. .env.local 不会被提交到 Git
#

# 个人本地后端地址（可选）
# 如果你的后端运行在不同端口，取消注释并修改
# VITE_API_BASE_URL=http://localhost:3000

# 个人测试用的 Firebase 项目（可选）
# 如果你有独立的测试 Firebase 项目，在此配置
# VITE_FIREBASE_API_KEY=your-test-api-key
# VITE_FIREBASE_PROJECT_ID=your-test-project
